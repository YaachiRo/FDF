
# include "fdf.h"

void get_z(t_fdf *fdf, matrex_t mt)
{
    int ***matrix_3d = malloc(sizeof(int **) * fdf->rows)
    int i = 0;
    int j = 0;

    while (i < fdf->rows)
    {
        j = 0;
        while(j < fdf->colums)
        {
            
            j++;
        }
        i++;
    }
}